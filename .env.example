# Copy to .env and adjust values
PORT=8080
MONGO_URI=
# Optional proxy for YoutubeExplode (host:port)
PROXY_URL=
# redirect | proxy | url
STREAM_MODE=redirect
# yt-dlp settings
USE_YTDLP=true
DOWNLOAD_TIMEOUT_SEC=240
DOWNLOAD_MAX_CONCURRENT=3
# YTDLP_PATH=yt-dlp
# YTDLP_USER_AGENT=Mozilla/5.0 (Linux; Android 10)
# YTDLP_COOKIES_PATH=/app/cookies.txt   # Ruta absoluta al cookies.txt (formato Netscape)
